spring.application.name=demo

# Database Configuration (Production-Ready)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://gateway01.us-west-2.prod.aws.tidbcloud.com:4000/test?sslMode=VERIFY_IDENTITY}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:3Nc2pAFbmaNnx13.root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:b2CxiKDka5WkwizR}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

# Permanent Admin Credentials (Environment-Driven)
spring.security.user.name=${ADMIN_USERNAME:admin}
spring.security.user.password=${ADMIN_PASSWORD:admin123}
spring.security.user.roles=SUPER_ADMIN

# Server Configuration
server.port=${PORT:8080}
